To protect the interests of plugin developers, we've optimized select premium plugins. 
These have been pruned for efficiency, meaning they would not load in the typical server environment but remain fully functional as APIs. 
Due to the absence of the required Maven repository, we're employing these streamlined jars for local dependency management.